import * as SQLite from 'expo-sqlite';
import { useState, useEffect } from 'react';
import { View, Text } from 'react-native';

export default function useDatabase() {
  
    /*
    // DB STUFF
    const dbname = 'mydb.db';
    const db= SQLite.openDatabaseSync('@/database/' + dbname); // Open the database
    const dbInitialQuery = 'CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, firstname TEXT, email TEXT, password Text)'; // Initial query to check if the table exists
  
  
    useEffect (() => {
      // Create a table if it doesn't exist
      db.execSync(dbInitialQuery); // Execute the initial query
    });*/
  
}
